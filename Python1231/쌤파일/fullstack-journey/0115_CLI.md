# CLI (Command-Line Interface)

## 💡 정의

정의:
- 터미널/명령 프롬프트에서 명령어를 입력해서 프로그램과 상호작용하는 방식
- GUI(그래픽 인터페이스)와 반대되는 개념
예시:
- git status → Git CLI
- python index.py add hi → 내가 만든 단어장 CLI
- npm install package → Node.js CLI

## 🎨 특징

text로 결과를 출력해줌
type이 string이라 계산식 등에서는 Number로 형 변환 해줘야함

## ⭐ Javascript에서 시작하기
- npm init . 해서 필요한 파일 다운받기
- package.json에서 type 모듈로 바꿔주기
- scripts에 사용할 키 만들어주기
![alt text](image-3.png)
- `node`는 Node.js 런타임을 실행하라는 명령어(js파일을 Node환경에서 실행할 때 사용)
- `npm run start`라고 start키를 사용하면 해당 폴더를 실행하겠다는 의미


### ✨ commander 알아보기 in JAVASCRIPT

#### 1️⃣ commander 라이브러리
- Node.js에서 CLI(Command Line Interface) 프로그램을 만들 때 사용하는 유명한 라이브러리
- 명령어, 옵션, 인자(argument)를 쉽게 정의하고 처리할 수 있게 해줌

#### 2️⃣ Command 클래스
Command는 commander에서 제공하는 주요 클래스
```
import { Command } from 'commander';

const program = new Command();

program
    .command('del')
    .argument('<word>')
    .description('지우기')
    .action(del);

program
    .command('update')
    .argument('<word>')
    .argument('<editWord>')
    .description('수정하기')
    .action(update);

program.parse(process.argv);
```

#### 3️⃣ 용어 정리

1. `const program = new Command();`
- `Command`는 commander 라이브러리에서 가져온 클래스
- 이 클래스를 새로운 인스턴스로 만들면 CLI 프로그램을 정의할 수 있는 객체가 생성됨 -> 그래서 메서드 사용 가능

2. `.name()`
- `program.name('my-cli');` 형태로 사용
- 역할: CLI 프로그램 이름 설정
- 효과: 사용자가 `my-cli --help`를 썼을 때 CLI 이름을 보여줌

3. `.description()`
- 역할: CLI 프로그램 설명
- 언제 쓰는지: 도움말(--help)을 통해 프로그램 목적을 안내할 때
- 효과: --help 실행 시 프로그램 설명이 나옴

4. `.argument`
- 역할: 명령어의 인자를 정의할 때 사용
- 사용자가 CLI 명령어 뒤에 값을 직접 입력할 수 있게 함 

5. `.option()`
- 역할: 명령어에 옵션(flag)을 추가
- 옵션 구성:
- `-u` → 단축 옵션
- `--uppercase` → 전체 옵션 이름
- `'Uppercase the greeting'` → 옵션 설명
사용법: 
```
my-cli greet Alice --uppercase
# 출력: HELLO, ALICE!
```
- 사용자가 CLI에서 선택적인 기능을 켜거나 끌 수 있게 함
- 예: --verbose → 자세한 로그 출력, --dry-run → 실제로 실행하지 않고 테스트

6. `.command()`
- 역할: CLI의 서브 명령어 정의
- CLI에서 action을 실행할 명령어

7. `.parse(process.argv)` **필수**
- 역할: 사용자가 입력한 명령어/옵션을 파싱하고 실행
- 마지막에 꼭 필요함: 이걸 안 쓰면 CLI가 입력을 처리하지 못함

-----

## ⭐ Python에서 시작하기
- uv init. : 파이썬에 프로젝트 생성
- 설정창 확인(pyprojcet.toml)
```
[project]
name = "0116"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"  #손대면 안되는 파일1
dependencies = []           #손대면 안되는 파일2: 외부 라이브러리 관련이라 건드리면 안됨
```
- uv run: 파이썬 실행
- uv run + 파일명: 해당 파일 실행
- uv run + 파일명 + CLI명령어: 해당 명령어가 가진 함수 실행

```
if __name__ == '__main__':
    t1()
```
👉 이 파일을 직접 실행했을 때만 아래 코드를 실행하라는 의미
➡ CLI 구조 깨지는 거 방지
➡ 모듈 재사용 가능
➡ 의도치 않은 실행 방지

### ✨ commander 알아보기 in PYTHON

```
import argparse
from words import list, insert, update, remove 
#다른 파일에 함수가 있다는 전제하에

parser = argparse.ArgumentParser(description="CLI Program")
subparsers = parser.add_subparsers(dest="command")

add_parser = subparsers.add_parser("list")
add_parser = subparsers.add_parser("insert")
add_parser.add_argument("word")
add_parser = subparsers.add_parser("update")
add_parser.add_argument("id")
add_parser.add_argument("word")
add_parser = subparsers.add_parser("delete")
add_parser.add_argument("id")

args = parser.parse_args()

if args.command == "list": list()
if args.command == "insert": insert(args.word)
if args.command == "update": update(args.id, args.word)
if args.command == "delete": remove(args.id)
```

- **for문으로 줄일 수 있음!**
```
DESC = "CLI Program"
commands = [
  {"command":"list", "arguments": [], "method": list},
  {"command":"add", "arguments": ["word"], "method": insert},
  {"command":"update", "arguments": ["id", "word"], "method": update},
  {"command":"delete", "arguments": ["id"], "method": remove}
]

for i in commands:
    add_parser=subparsers.add_parser(i['command'])
    for j in i['arguments']:
        add_parser.add_argument(j)

for i in commands:
    if i.command == i['command']
        i['method'](args)
        breack
```

#### 📖 용어 해석

- `argparse`: 파이썬 기본 내장 CLI 라이브러리
    -✔ 역할:
        - 터미널에서 입력한 걸 해석
        - 옵션, 인자, 명령어 처리
        - 자동 --help 생성

- `ArgumentParser`: CLI 해석기(설계도)
    -✔ 역할
        - 전체 CLI 프로그램을 대표
        - 도움말(--help) 관리
        - 명령어, 옵션을 담는 컨테이너

- `parser`: ArgumentParser로 만든 객체(실체)
    - ArgumentParser → 클래스
    - parser → 실제 사용 인스턴스
- `add_subparser`: 명령어 그룹을 만드는 함수
    - ✔ 역할
        - list, add, update, delete 같은 서브 명령어 생성
        - 어떤 명령이 실행됐는지 저장
- `subparsers`:각 command 전용 parser
    - ✔ 역할
        - 서브명령어의 명령어 전용 설정 공간
        - 서브명령어에만 필요한 인자 등록
- `add_argument`: 명령어 뒤에 오는 값 정의
    - ✔ 역할
        - 필수 인자 정의
        - 터미널에서 입력받은 값 저장
- `command`: 사용자가 실행한 명령어 이름