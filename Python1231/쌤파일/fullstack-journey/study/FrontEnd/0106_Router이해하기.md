## 📝 Export / Import 헷갈렸던 부분 추가
### 1️⃣ Default Export / Import
- **파일당 하나만 export 가능**
```
* Update라는 이름의 페이지
const Update()=>{return(내용)}

export default Update


*받아오는 대표 페이지
import Update(이름) from "./Update(파일명).jsx"
```
- 위와 같은 형태로 중괄호 없이 받아옴
- import할 때 이름 자유롭게 변경 가능
- 해당 파일위치에 default값이 하나만 존재하기 때문에 이름을 뭘로 받아와도 상관어지는 것임

### 2️⃣ Named Export / Import
```
* Not_Footer라는 이름의 페이지
export const NotFound = () => {...}
export const Footer = () => {...}


*받아오는 대표 페이지
import { NotFound, Footer } from "./Not_Footer.jsx
```
- 파일 안에 여러 개의 컴포넌트를 export 가능  
- import 시 **{} 안에 정확한 이름** 사용 
- 여러개 중 원하는 걸 뽑아와야해서 이름이 정확해야함

3. ### 3️⃣ 외부 라이브러리 import

```import { BrowserRouter, Routes, Route } from "react-router";```

- React, Router, Bootstrap 등의 기능을 불러와서 사용 가능  

---

## 2. map 함수 완벽 사용

- **배열명.map((받는 값)=>(재배열할 상태))**

```
const menu = [
    {path: "/new", lable:"Create"},
    {path: "/detail", lable:"Detail"},
    {path: "/edit", lable:"Update"},
    {path: "/404", lable:"404"},
  ]

   {
    menu.map((v,i)=>(
    <li key={i} className="nav-item">
    <a className="nav-link" href={v.path}>{v.lable}</a>
    </li>
    ))
    }
```
- `(v, i)` → 배열의 값과 인덱스 (`meun[i]`의 값 `v`)
- `key`는 **반드시 가장 상위 태그에**  
  - 가능하면 **고유 id** 사용, index는 추가/삭제 시 오류 발생 가능  
- 재배열할 상태에는 구현해야하는 모양 그대로 집어넣어줘야함.
-변동이 있는 값만 useState로 관리해주고 재배열 시 `{v.name}`으로 꺼내 바꿔줄 수 있음
- 점 연산자(`form.name`)가 있는 경우   

💡map으로 채우는데 ` value={form.name}` 이렇게 점연산자가 있는 애들은 어떻게 하나요?
`form.name = form[name]` 이 두 값이 같다는 것을 활용해
`value={form[v.name]}` 이렇게 사용해 줄 수 있다.

---

## 3. useEffect 사용
### 형태
```
useEffect(()=> {
    setName: name,
    setEmail: email,
},[])
```
`[]`로 어떤 값의 변동 시 이펙트를 발생시킬 것인지 정해줌
- `[]` 맨 처음 렌딩시에만 useEffect 내부 코드 실행
- `[특정state]` 해당 state에 변동이 생기면 그때마다 리렌딩해줌
- 아예 없을 때: 함수 속 어떤 것이든 값 변화가 생기면 useEffect내부 함수 실행


#### 불러올 state가 빈 값일 때는요???
```
useEffect(()=> {
    if(name)
    setName: name || "",
    setEmail: email || "",
},[])
```
- if문으로 name이 있을 때 만 실행하도록 할 수 있고,
name, email이 있으면 채우고 없으면 빈 문자열로 채워주세요도 가능

---

## 4. onSubmit
```
<form onSubmit={handleSubmit}>
  <button type="submit">전송</button>
</form>
```
- 값 담아 전송할 때 사용
- form에 `onSubmit={함수}`를 넣어주고
- `button type="submit"`으로 지정해줌
이때!! **button은 꼭 form안에 들어있어야함.** ->onSubmit에 값 넘기기 위해서
- `onSubmit`으로 실행할 함수에는 반드시
`e.preventDefault()` 넣어줘야함
- `onSubmit`이 가진 자동 새로고침 특성을 억제시켜줌

---

## 5. React state에서 prev 사용

- `prev` → **이전 state 값**, 항상 최신 state 기준

```
setArrs(arrs.filter((_, i) => i !== index))
setArrs(arrs.filter((_, i) => i !== index2))
```
이렇게 있다고 하면 두번째 줄의 arrs는 첫줄이 실행되기 전의 값을 참조해서 버그 생길 수 있음  

그래서 
```
setArrs( prev => prev.filter((_, i) => i !== index))
setArrs( prev => prev.filter((_, i) => i !== index2))
```
로 쓰는게 좋음  

- **사용 가능한 경우**
  - 배열: map, filter, concat, push, spread  
  - 객체 업데이트: `{ ...prev, key: value }