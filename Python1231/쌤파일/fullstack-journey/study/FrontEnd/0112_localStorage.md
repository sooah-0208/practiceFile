# localStorage & JSON 정리 📝

## 브라우저에 값을 저장하고 싶을 때 사용

### 1️⃣ localStorage
- 정의: 브라우저에서 자체적으로 제공하는 **Web Storage 객체**  
- 용도: 브라우저에 문자열을 저장하는 도구  
- 특징:
  - 문자열이 아닌 값들을 저장하고 싶으면 **모든 값을 문자열로 변환**해야 함  
  - 객체나 배열을 저장할 때는 **JSON과 함께 사용** 
  - 객체이기 때문에 항상 매서드와 함께 사용해야함!

#### 주요 메서드
- localStorage.setItem("key", value): 데이터 저장
- localStorage.getItem("key")       : 데이터 가져오기
- localStorage.removeItem("key")    : 특정 데이터 삭제
- localStorage.clear()            : 전체 삭제

---

### 2️⃣ JSON

- 정의: 전역 객체(Global Object)  
- 용도: **객체/배열 ↔ 문자열** 변환 도구  
- 특징:
  - 항상 전용 메서드와 함께 사용
  - 문자열을 객체/배열로 바꿀 때는 **규칙을 지켜야 함**
    - 아니면 파싱 시 오류 발생

#### 주요 메서드
- JSON.stringify(obj): 객체/배열 -> 문자열
- JSON.parse(str)    : 문자열 -> 객체/배열

#### 사용 예시

- 💾 객체를 문자열로 변환 후 localStorage에 저장:
```
const user = {name: "Alice", age: 20}

 useEffect(()=>{
        localStorage.setItem("user", JSON.stringify(user))
    },[user])
```

- 📥 localStorage에서 불러와서 객체로 변환:
``` 
    useEffect(()=>{
        const savedUser = localStorage.getItem("user")
        if(savedUser){
            setUser(JSON.parse(savedUser))
        }
    },[])
```

- ⚠️ 규칙 주의:
  - 문자열 안에 객체/배열 형태를 **반드시 JSON 규칙에 맞게 작성**해야 함
- 올바른 예: `"{\"a\":1, \"b\":2}"`, `"[{\"a\":1}, 2, 3]"`  
- 틀린 예: `"{a, b, c}"`, `"[{a, b}, c, d]"` → 에러 발생
